/*
 * Objectos y Pantallas
 */


/*
 * Objeto Screen
 * 
 */
// height					:(retorna la dimension vertical de la resolucion del monitor)
// width					:(retorna la dimension horizontal de la resolucion del monitor)

/*
 * Objeto Location
 * 
 */
// host						:(retorna el nombre del host y la puerta de enlace)
// hostname					:(retorna el nombre del host solo)
// href						:(retorna la URL completa)
// pathname					:(retorna la direccion del host)
// search					:(parametros vis get)
// assign					:(carga un documento nuevo)
// reload					:(recarga el documento actual)
// replace					:(carga un documento nuevo y sin dejar historico)


/*
 * Objeto Window
 * 
 */
// opener					:(retorna una referencia para una vista que abre una vista actual)
// name						:(atribuye/retorna el nombre de la vista)
// focus					:(atrubuye el foco a la vista actual)
// blur						:(remueve el foco a la vista actual)
// back						:(retorna a una pagina anterior del historico)
// forward					:(retorna a una pagina siguiente del historico)
// clear/setInterval()		:(ejecuta una funcion cada periodo)
// clear/setTimeout()		:(ejecuta una funcion en un determinado periodo)
// moveBy()					:(mueve una pantalla)
// movetO()					:(mueve una pantalla a una posicion)
// open()					:(abre una nueva pantalla)
// alert()					:(emite un mensaje de usuario)
// confirm() 			    :(Ofrece la posibilidad de responder positiva o negativamente (si/no) una pregunta)
// prompt() 				:(Ofrece la posibilidad de responder preguntas discursivamente)

/*
 * innerHTML : propiedad que retorna o atribuye HTML a un determinado elemento
 * 
 */



